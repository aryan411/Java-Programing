--- SEQ PLAYER ---
   CREATE SEQUENCE  "COMP214_W22_ZOR_49"."PLAYER_PLAYER_ID_SEQ"  MINVALUE 1 MAXVALUE 99999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOPARTITION ;

--- TABLE PLAYER ---
CREATE TABLE PLAYER 
   (	"PLAYER_ID" NUMBER DEFAULT PLAYER_PLAYER_ID_SEQ.NEXTVAL NOT NULL ENABLE,
	"FIRST_NAME" VARCHAR2(30 BYTE) NOT NULL ENABLE, 
	"LAST_NAME" VARCHAR2(30 BYTE), 
	"ADDRESS" VARCHAR2(50 BYTE), 
    "POSTAL_CODE" VARCHAR2(30 BYTE),
    "PROVINCE" VARCHAR2(30 BYTE),
	"PHONE_NUMBER" NUMBER(10,0),  
	 CONSTRAINT "PLAYER_PK" PRIMARY KEY ("PLAYER_ID"));



--- SEQ GAME ---
   CREATE SEQUENCE  "COMP214_W22_ZOR_49"."GAME_GAME_ID_SEQ"  MINVALUE 1 MAXVALUE 99999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOPARTITION ;

--- TABLE GAME ---
CREATE TABLE GAME 
   (	"GAME_ID" NUMBER DEFAULT GAME_GAME_ID_SEQ.NEXTVAL NOT NULL ENABLE, 
	"GAME_TITLE" VARCHAR2(30 BYTE), 
	 CONSTRAINT "GAME_PK" PRIMARY KEY ("GAME_ID"));

--- SEQ PLAYERGAME_ID ---
CREATE SEQUENCE  PLAYERGAME_ID_SEQ  
    MINVALUE 1 
    MAXVALUE 99999 
    INCREMENT BY 1 
    START WITH 1 
    CACHE 20 
    NOORDER  
    NOCYCLE
    NOPARTITION ;

--- TABLE PLAYERANDGAME ---
CREATE TABLE PLAYERANDGAME 
   (	"PLAYER_GAME_ID" NUMBER DEFAULT PLAYERGAME_ID_SEQ.NEXTVAL NOT NULL ENABLE, 
	"GAME_ID" NUMBER NOT NULL ENABLE,
    "PLAYER_ID" NUMBER NOT NULL ENABLE,
    "PLAYING_DATE" DATE NOT NULL ENABLE,
    "SCORE" NUMBER NOT NULL ENABLE, 
	 CONSTRAINT "PLAYERANDGAME_FK1" FOREIGN KEY ("GAME_ID")
	  REFERENCES GAME ("GAME_ID") ENABLE, 
	 CONSTRAINT "PLAYERANDGAME_FK2" FOREIGN KEY ("PLAYER_ID")
	  REFERENCES PLAYER ("PLAYER_ID") ENABLE
   );
   